name: Build Chromium Binaries

on:
  push:
    branches:    
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    container: eclipse/platformreleng-centos-swt-build:gerrit

    steps:
    - run: dir
    - run: pwd
